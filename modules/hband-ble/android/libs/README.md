# AARs (Veepoo SDK)

`vpbluetooth-1.17.aar`, `vpprotocol-2.3.21.15.aar` are copied from project root `h-band/android/libs/` so that EAS Build and local builds resolve them without relying on that path. To update, copy again from `h-band/android/libs/`.
